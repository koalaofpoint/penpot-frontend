["^ ","~:resource-id",["~:shadow.build.npm/resource","node_modules/lodash/debounce.js"],"~:js","shadow$provide[228]=function(d,z,e){var g=d(215),p=d(218),h=d(227),m=Math.max,r=Math.min;z.exports=function(t,y,x){function B(pa){var Ga=K,Ra=O;K=O=void 0;ha=pa;return P=t.apply(Ra,Ga)}function C(pa){var Ga=pa-Z;pa-=ha;return Z===void 0||Ga>=y||Ga<0||R&&pa>=Q}function L(){var pa=p();if(C(pa))return G(pa);var Ga=setTimeout;var Ra=pa-ha;pa=y-(pa-Z);Ra=R?r(pa,Q-Ra):pa;ca=Ga(L,Ra)}function G(pa){ca=void 0;if(V&&K)return B(pa);K=O=void 0;return P}function M(){var pa=p(),Ga=C(pa);K=arguments;O=this;Z=pa;\nif(Ga){if(ca===void 0)return ha=pa=Z,ca=setTimeout(L,y),sa?B(pa):P;if(R)return clearTimeout(ca),ca=setTimeout(L,y),B(Z)}ca===void 0&&(ca=setTimeout(L,y));return P}var K,O,P,ca,Z,ha=0,sa=!1,R=!1,V=!0;if(typeof t!=\"function\")throw new TypeError(\"Expected a function\");y=h(y)||0;if(g(x)){sa=!!x.leading;var Q=(R=\"maxWait\"in x)?m(h(x.maxWait)||0,y):Q;V=\"trailing\"in x?!!x.trailing:V}M.cancel=function(){ca!==void 0&&clearTimeout(ca);ha=0;K=Z=O=ca=void 0};M.flush=function(){return ca===void 0?P:G(p())};return M}}","~:source","shadow$provide[228] = function(require,module,exports) {\nvar isObject = require('./isObject'),\n    now = require('./now'),\n    toNumber = require('./toNumber');\n\n/** Error message constants. */\nvar FUNC_ERROR_TEXT = 'Expected a function';\n\n/* Built-in method references for those with the same name as other `lodash` methods. */\nvar nativeMax = Math.max,\n    nativeMin = Math.min;\n\n/**\n * Creates a debounced function that delays invoking `func` until after `wait`\n * milliseconds have elapsed since the last time the debounced function was\n * invoked. The debounced function comes with a `cancel` method to cancel\n * delayed `func` invocations and a `flush` method to immediately invoke them.\n * Provide `options` to indicate whether `func` should be invoked on the\n * leading and/or trailing edge of the `wait` timeout. The `func` is invoked\n * with the last arguments provided to the debounced function. Subsequent\n * calls to the debounced function return the result of the last `func`\n * invocation.\n *\n * **Note:** If `leading` and `trailing` options are `true`, `func` is\n * invoked on the trailing edge of the timeout only if the debounced function\n * is invoked more than once during the `wait` timeout.\n *\n * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred\n * until to the next tick, similar to `setTimeout` with a timeout of `0`.\n *\n * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)\n * for details over the differences between `_.debounce` and `_.throttle`.\n *\n * @static\n * @memberOf _\n * @since 0.1.0\n * @category Function\n * @param {Function} func The function to debounce.\n * @param {number} [wait=0] The number of milliseconds to delay.\n * @param {Object} [options={}] The options object.\n * @param {boolean} [options.leading=false]\n *  Specify invoking on the leading edge of the timeout.\n * @param {number} [options.maxWait]\n *  The maximum time `func` is allowed to be delayed before it's invoked.\n * @param {boolean} [options.trailing=true]\n *  Specify invoking on the trailing edge of the timeout.\n * @returns {Function} Returns the new debounced function.\n * @example\n *\n * // Avoid costly calculations while the window size is in flux.\n * jQuery(window).on('resize', _.debounce(calculateLayout, 150));\n *\n * // Invoke `sendMail` when clicked, debouncing subsequent calls.\n * jQuery(element).on('click', _.debounce(sendMail, 300, {\n *   'leading': true,\n *   'trailing': false\n * }));\n *\n * // Ensure `batchLog` is invoked once after 1 second of debounced calls.\n * var debounced = _.debounce(batchLog, 250, { 'maxWait': 1000 });\n * var source = new EventSource('/stream');\n * jQuery(source).on('message', debounced);\n *\n * // Cancel the trailing debounced invocation.\n * jQuery(window).on('popstate', debounced.cancel);\n */\nfunction debounce(func, wait, options) {\n  var lastArgs,\n      lastThis,\n      maxWait,\n      result,\n      timerId,\n      lastCallTime,\n      lastInvokeTime = 0,\n      leading = false,\n      maxing = false,\n      trailing = true;\n\n  if (typeof func != 'function') {\n    throw new TypeError(FUNC_ERROR_TEXT);\n  }\n  wait = toNumber(wait) || 0;\n  if (isObject(options)) {\n    leading = !!options.leading;\n    maxing = 'maxWait' in options;\n    maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;\n    trailing = 'trailing' in options ? !!options.trailing : trailing;\n  }\n\n  function invokeFunc(time) {\n    var args = lastArgs,\n        thisArg = lastThis;\n\n    lastArgs = lastThis = undefined;\n    lastInvokeTime = time;\n    result = func.apply(thisArg, args);\n    return result;\n  }\n\n  function leadingEdge(time) {\n    // Reset any `maxWait` timer.\n    lastInvokeTime = time;\n    // Start the timer for the trailing edge.\n    timerId = setTimeout(timerExpired, wait);\n    // Invoke the leading edge.\n    return leading ? invokeFunc(time) : result;\n  }\n\n  function remainingWait(time) {\n    var timeSinceLastCall = time - lastCallTime,\n        timeSinceLastInvoke = time - lastInvokeTime,\n        timeWaiting = wait - timeSinceLastCall;\n\n    return maxing\n      ? nativeMin(timeWaiting, maxWait - timeSinceLastInvoke)\n      : timeWaiting;\n  }\n\n  function shouldInvoke(time) {\n    var timeSinceLastCall = time - lastCallTime,\n        timeSinceLastInvoke = time - lastInvokeTime;\n\n    // Either this is the first call, activity has stopped and we're at the\n    // trailing edge, the system time has gone backwards and we're treating\n    // it as the trailing edge, or we've hit the `maxWait` limit.\n    return (lastCallTime === undefined || (timeSinceLastCall >= wait) ||\n      (timeSinceLastCall < 0) || (maxing && timeSinceLastInvoke >= maxWait));\n  }\n\n  function timerExpired() {\n    var time = now();\n    if (shouldInvoke(time)) {\n      return trailingEdge(time);\n    }\n    // Restart the timer.\n    timerId = setTimeout(timerExpired, remainingWait(time));\n  }\n\n  function trailingEdge(time) {\n    timerId = undefined;\n\n    // Only invoke if we have `lastArgs` which means `func` has been\n    // debounced at least once.\n    if (trailing && lastArgs) {\n      return invokeFunc(time);\n    }\n    lastArgs = lastThis = undefined;\n    return result;\n  }\n\n  function cancel() {\n    if (timerId !== undefined) {\n      clearTimeout(timerId);\n    }\n    lastInvokeTime = 0;\n    lastArgs = lastCallTime = lastThis = timerId = undefined;\n  }\n\n  function flush() {\n    return timerId === undefined ? result : trailingEdge(now());\n  }\n\n  function debounced() {\n    var time = now(),\n        isInvoking = shouldInvoke(time);\n\n    lastArgs = arguments;\n    lastThis = this;\n    lastCallTime = time;\n\n    if (isInvoking) {\n      if (timerId === undefined) {\n        return leadingEdge(lastCallTime);\n      }\n      if (maxing) {\n        // Handle invocations in a tight loop.\n        clearTimeout(timerId);\n        timerId = setTimeout(timerExpired, wait);\n        return invokeFunc(lastCallTime);\n      }\n    }\n    if (timerId === undefined) {\n      timerId = setTimeout(timerExpired, wait);\n    }\n    return result;\n  }\n  debounced.cancel = cancel;\n  debounced.flush = flush;\n  return debounced;\n}\n\nmodule.exports = debounce;\n\n};","~:removed-requires",["~#set",[]],"~:actual-requires",["^5",["~$shadow.js","~$module$node_modules$lodash$isObject","~$module$node_modules$lodash$toNumber","~$module$node_modules$lodash$now"]],"~:properties",["^5",["cancel","flush"]],"~:compiled-at",1780648643198,"~:source-map-json","{\n\"version\":3,\n\"file\":\"module$node_modules$lodash$debounce.js\",\n\"lineCount\":2,\n\"mappings\":\"AAAAA,cAAA,CAAe,GAAf,CAAA,CAAsB,QAAQ,CAACC,CAAD,CAASC,CAAT,CAAgBC,CAAhB,CAAyB,CAAA,IACnDC,EAAWH,CAAA,CAAQ,GAAR,CADwC,CAEnDI,EAAMJ,CAAA,CAAQ,GAAR,CAF6C,CAGnDK,EAAWL,CAAA,CAAQ,GAAR,CAHwC,CASnDM,EAAYC,IAAKC,CAAAA,GATkC,CAUnDC,EAAYF,IAAKG,CAAAA,GAqLrBT,EAAOC,CAAAA,OAAP,CA7HAS,QAAiB,CAACC,CAAD,CAAOC,CAAP,CAAaC,CAAb,CAAsB,CAuBrCC,QAASA,EAAU,CAACC,EAAD,CAAO,CAAA,IACpBC,GAAOC,CADa,CAEpBC,GAAUC,CAEdF,EAAA,CAAWE,CAAX,CAAsBC,IAAAA,EACtBC,GAAA,CAAiBN,EAEjB,OADAO,EACA,CADSX,CAAKY,CAAAA,KAAL,CAAWL,EAAX,CAAoBF,EAApB,CANe,CA6B1BQ,QAASA,EAAY,CAACT,EAAD,CAAO,CAAA,IACtBU,GAAoBV,EAApBU,CAA2BC,CACLX,GAAtBY,EAA6BN,EAKjC,OAAQK,EAAR,GAAyBN,IAAAA,EAAzB,EAAuCK,EAAvC,EAA4Db,CAA5D,EACGa,EADH,CACuB,CADvB,EAC8BG,CAD9B,EACwCD,EADxC,EAC+DE,CARrC,CAW5BC,QAASA,EAAY,EAAG,CACtB,IAAIf,GAAOZ,CAAA,EACX,IAAIqB,CAAA,CAAaT,EAAb,CAAJ,CACE,MAAOgB,EAAA,CAAahB,EAAb,CAGCiB,KAAAA,GAAAA,UAzBNL,KAAAA,GAyB6CZ,EAzB7CY,CAA6BN,EAC7BY,GAAAA,CAAcrB,CAAdqB,EAwB6ClB,EAxB7CkB,CAF2BP,CAE3BO,CAEJ,GAAA,CAAOL,CAAA,CACHpB,CAAA,CAAUyB,EAAV,CAAuBJ,CAAvB,CAAiCF,EAAjC,CADG,CAEHM,EAoBJC,GAAA,CAAUF,EAAA,CAAWF,CAAX,CAAyB,EAAzB,CANY,CASxBC,QAASA,EAAY,CAAChB,EAAD,CAAO,CAC1BmB,EAAA,CAAUd,IAAAA,EAIV,IAAIe,CAAJ,EAAgBlB,CAAhB,CACE,MAAOH,EAAA,CAAWC,EAAX,CAETE,EAAA,CAAWE,CAAX,CAAsBC,IAAAA,EACtB,OAAOE,EATmB,CAwB5Bc,QAASA,EAAS,EAAG,CAAA,IACfrB,GAAOZ,CAAA,EADQ,CAEfkC,GAAab,CAAA,CAAaT,EAAb,CAEjBE,EAAA,CAAWqB,SACXnB,EAAA,CAAW,IACXO,EAAA,CAAeX,EAEf;GAAIsB,EAAJ,CAAgB,CACd,GAAIH,EAAJ,GAAgBd,IAAAA,EAAhB,CACE,MAvEJC,GAIO,CAmEgBK,EAnEhB,CAmEgBA,CAnEhB,CAFPQ,EAEO,CAFGF,UAAA,CAAWF,CAAX,CAAyBlB,CAAzB,CAEH,CAAA2B,EAAA,CAAUzB,CAAA,CAAWC,EAAX,CAAV,CAA6BO,CAqElC,IAAIM,CAAJ,CAIE,MAFAY,aAAA,CAAaN,EAAb,CAEO,CADPA,EACO,CADGF,UAAA,CAAWF,CAAX,CAAyBlB,CAAzB,CACH,CAAAE,CAAA,CAAWY,CAAX,CARK,CAWZQ,EAAJ,GAAgBd,IAAAA,EAAhB,GACEc,EADF,CACYF,UAAA,CAAWF,CAAX,CAAyBlB,CAAzB,CADZ,CAGA,OAAOU,EAtBY,CAhGgB,IACjCL,CADiC,CAEjCE,CAFiC,CAIjCG,CAJiC,CAKjCY,EALiC,CAMjCR,CANiC,CAOjCL,GAAiB,CAPgB,CAQjCkB,GAAU,CAAA,CARuB,CASjCX,EAAS,CAAA,CATwB,CAUjCO,EAAW,CAAA,CAEf,IAAI,MAAOxB,EAAX,EAAmB,UAAnB,CACE,KAAM,KAAI8B,SAAJ,CAzEYC,qBAyEZ,CAAN,CAEF9B,CAAA,CAAOR,CAAA,CAASQ,CAAT,CAAP,EAAyB,CACzB,IAAIV,CAAA,CAASW,CAAT,CAAJ,CAAuB,CACrB0B,EAAA,CAAU,CAAC,CAAC1B,CAAQ0B,CAAAA,OAEpB,KAAAV,EAAU,CADVD,CACU,CADD,SACC,EADYf,EACZ,EAASR,CAAA,CAAUD,CAAA,CAASS,CAAQgB,CAAAA,OAAjB,CAAV,EAAuC,CAAvC,CAA0CjB,CAA1C,CAAT,CAA2DiB,CACrEM,EAAA,CAAW,UAAA,EAActB,EAAd,CAAwB,CAAC,CAACA,CAAQsB,CAAAA,QAAlC,CAA6CA,CAJnC,CAwGvBC,CAAUO,CAAAA,MAAV,CApCAA,QAAe,EAAG,CACZT,EAAJ,GAAgBd,IAAAA,EAAhB,EACEoB,YAAA,CAAaN,EAAb,CAEFb,GAAA,CAAiB,CACjBJ,EAAA,CAAWS,CAAX,CAA0BP,CAA1B,CAAqCe,EAArC,CAA+Cd,IAAAA,EAL/B,CAqClBgB,EAAUQ,CAAAA,KAAV,CA7BAA,QAAc,EAAG,CACf,MAAOV,GAAA,GAAYd,IAAAA,EAAZ,CAAwBE,CAAxB,CAAiCS,CAAA,CAAa5B,CAAA,EAAb,CADzB,CA8BjB,OAAOiC,EA1H8B,CAlEgB;\",\n\"sources\":[\"node_modules/lodash/debounce.js\"],\n\"sourcesContent\":[\"shadow$provide[228] = function(require,module,exports) {\\nvar isObject = require('./isObject'),\\n    now = require('./now'),\\n    toNumber = require('./toNumber');\\n\\n/** Error message constants. */\\nvar FUNC_ERROR_TEXT = 'Expected a function';\\n\\n/* Built-in method references for those with the same name as other `lodash` methods. */\\nvar nativeMax = Math.max,\\n    nativeMin = Math.min;\\n\\n/**\\n * Creates a debounced function that delays invoking `func` until after `wait`\\n * milliseconds have elapsed since the last time the debounced function was\\n * invoked. The debounced function comes with a `cancel` method to cancel\\n * delayed `func` invocations and a `flush` method to immediately invoke them.\\n * Provide `options` to indicate whether `func` should be invoked on the\\n * leading and/or trailing edge of the `wait` timeout. The `func` is invoked\\n * with the last arguments provided to the debounced function. Subsequent\\n * calls to the debounced function return the result of the last `func`\\n * invocation.\\n *\\n * **Note:** If `leading` and `trailing` options are `true`, `func` is\\n * invoked on the trailing edge of the timeout only if the debounced function\\n * is invoked more than once during the `wait` timeout.\\n *\\n * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred\\n * until to the next tick, similar to `setTimeout` with a timeout of `0`.\\n *\\n * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)\\n * for details over the differences between `_.debounce` and `_.throttle`.\\n *\\n * @static\\n * @memberOf _\\n * @since 0.1.0\\n * @category Function\\n * @param {Function} func The function to debounce.\\n * @param {number} [wait=0] The number of milliseconds to delay.\\n * @param {Object} [options={}] The options object.\\n * @param {boolean} [options.leading=false]\\n *  Specify invoking on the leading edge of the timeout.\\n * @param {number} [options.maxWait]\\n *  The maximum time `func` is allowed to be delayed before it's invoked.\\n * @param {boolean} [options.trailing=true]\\n *  Specify invoking on the trailing edge of the timeout.\\n * @returns {Function} Returns the new debounced function.\\n * @example\\n *\\n * // Avoid costly calculations while the window size is in flux.\\n * jQuery(window).on('resize', _.debounce(calculateLayout, 150));\\n *\\n * // Invoke `sendMail` when clicked, debouncing subsequent calls.\\n * jQuery(element).on('click', _.debounce(sendMail, 300, {\\n *   'leading': true,\\n *   'trailing': false\\n * }));\\n *\\n * // Ensure `batchLog` is invoked once after 1 second of debounced calls.\\n * var debounced = _.debounce(batchLog, 250, { 'maxWait': 1000 });\\n * var source = new EventSource('/stream');\\n * jQuery(source).on('message', debounced);\\n *\\n * // Cancel the trailing debounced invocation.\\n * jQuery(window).on('popstate', debounced.cancel);\\n */\\nfunction debounce(func, wait, options) {\\n  var lastArgs,\\n      lastThis,\\n      maxWait,\\n      result,\\n      timerId,\\n      lastCallTime,\\n      lastInvokeTime = 0,\\n      leading = false,\\n      maxing = false,\\n      trailing = true;\\n\\n  if (typeof func != 'function') {\\n    throw new TypeError(FUNC_ERROR_TEXT);\\n  }\\n  wait = toNumber(wait) || 0;\\n  if (isObject(options)) {\\n    leading = !!options.leading;\\n    maxing = 'maxWait' in options;\\n    maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;\\n    trailing = 'trailing' in options ? !!options.trailing : trailing;\\n  }\\n\\n  function invokeFunc(time) {\\n    var args = lastArgs,\\n        thisArg = lastThis;\\n\\n    lastArgs = lastThis = undefined;\\n    lastInvokeTime = time;\\n    result = func.apply(thisArg, args);\\n    return result;\\n  }\\n\\n  function leadingEdge(time) {\\n    // Reset any `maxWait` timer.\\n    lastInvokeTime = time;\\n    // Start the timer for the trailing edge.\\n    timerId = setTimeout(timerExpired, wait);\\n    // Invoke the leading edge.\\n    return leading ? invokeFunc(time) : result;\\n  }\\n\\n  function remainingWait(time) {\\n    var timeSinceLastCall = time - lastCallTime,\\n        timeSinceLastInvoke = time - lastInvokeTime,\\n        timeWaiting = wait - timeSinceLastCall;\\n\\n    return maxing\\n      ? nativeMin(timeWaiting, maxWait - timeSinceLastInvoke)\\n      : timeWaiting;\\n  }\\n\\n  function shouldInvoke(time) {\\n    var timeSinceLastCall = time - lastCallTime,\\n        timeSinceLastInvoke = time - lastInvokeTime;\\n\\n    // Either this is the first call, activity has stopped and we're at the\\n    // trailing edge, the system time has gone backwards and we're treating\\n    // it as the trailing edge, or we've hit the `maxWait` limit.\\n    return (lastCallTime === undefined || (timeSinceLastCall >= wait) ||\\n      (timeSinceLastCall < 0) || (maxing && timeSinceLastInvoke >= maxWait));\\n  }\\n\\n  function timerExpired() {\\n    var time = now();\\n    if (shouldInvoke(time)) {\\n      return trailingEdge(time);\\n    }\\n    // Restart the timer.\\n    timerId = setTimeout(timerExpired, remainingWait(time));\\n  }\\n\\n  function trailingEdge(time) {\\n    timerId = undefined;\\n\\n    // Only invoke if we have `lastArgs` which means `func` has been\\n    // debounced at least once.\\n    if (trailing && lastArgs) {\\n      return invokeFunc(time);\\n    }\\n    lastArgs = lastThis = undefined;\\n    return result;\\n  }\\n\\n  function cancel() {\\n    if (timerId !== undefined) {\\n      clearTimeout(timerId);\\n    }\\n    lastInvokeTime = 0;\\n    lastArgs = lastCallTime = lastThis = timerId = undefined;\\n  }\\n\\n  function flush() {\\n    return timerId === undefined ? result : trailingEdge(now());\\n  }\\n\\n  function debounced() {\\n    var time = now(),\\n        isInvoking = shouldInvoke(time);\\n\\n    lastArgs = arguments;\\n    lastThis = this;\\n    lastCallTime = time;\\n\\n    if (isInvoking) {\\n      if (timerId === undefined) {\\n        return leadingEdge(lastCallTime);\\n      }\\n      if (maxing) {\\n        // Handle invocations in a tight loop.\\n        clearTimeout(timerId);\\n        timerId = setTimeout(timerExpired, wait);\\n        return invokeFunc(lastCallTime);\\n      }\\n    }\\n    if (timerId === undefined) {\\n      timerId = setTimeout(timerExpired, wait);\\n    }\\n    return result;\\n  }\\n  debounced.cancel = cancel;\\n  debounced.flush = flush;\\n  return debounced;\\n}\\n\\nmodule.exports = debounce;\\n\\n};\"],\n\"names\":[\"shadow$provide\",\"require\",\"module\",\"exports\",\"isObject\",\"now\",\"toNumber\",\"nativeMax\",\"Math\",\"max\",\"nativeMin\",\"min\",\"debounce\",\"func\",\"wait\",\"options\",\"invokeFunc\",\"time\",\"args\",\"lastArgs\",\"thisArg\",\"lastThis\",\"undefined\",\"lastInvokeTime\",\"result\",\"apply\",\"shouldInvoke\",\"timeSinceLastCall\",\"lastCallTime\",\"timeSinceLastInvoke\",\"maxing\",\"maxWait\",\"timerExpired\",\"trailingEdge\",\"setTimeout\",\"timeWaiting\",\"timerId\",\"trailing\",\"debounced\",\"isInvoking\",\"arguments\",\"leading\",\"clearTimeout\",\"TypeError\",\"FUNC_ERROR_TEXT\",\"cancel\",\"flush\"]\n}\n"]